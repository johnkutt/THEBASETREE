{
  "name": "thebasetree-inception",
  "version": "1.0.0",
  "description": "Inception Memory Framework - Layered simulation system for TheBaseTree",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy-inception.js --network baseSepolia",
    "deploy-local": "hardhat run scripts/deploy-inception.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
